use dotenvy::dotenv;
use std::env;
use arrow::record_batch::RecordBatch;

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    // ────────────────────────────────────────────────────────────────
    // 1. Load .env file into std::env
    //    This makes all variables in your `.env` file available via env::var()
    // ────────────────────────────────────────────────────────────────
    dotenv().ok();

    // ────────────────────────────────────────────────────────────────
    // 2. Read MinIO / S3 configuration values from the environment
    //    These should be defined in your .env file.
    // ────────────────────────────────────────────────────────────────
    let endpoint = env::var("AWS_ENDPOINT_URL")?;
    let access_key = env::var("AWS_ACCESS_KEY_ID")?;
    let secret_key = env::var("AWS_SECRET_ACCESS_KEY")?;
    let region = env::var("AWS_REGION")?;
    let bucket = env::var("MINIO_BUCKET")?;

    // ────────────────────────────────────────────────────────────────
    // 3. Optionally set the CA bundle path (if you're using HTTPS)
    //    reqwest (used by object_store) will automatically trust this CA
    // ────────────────────────────────────────────────────────────────
    if let Ok(ca_bundle) = env::var("AWS_CA_BUNDLE") {
        env::set_var("AWS_CA_BUNDLE", &ca_bundle);
        println!("🔐 Using custom CA bundle at: {}", ca_bundle);
    }

    // ────────────────────────────────────────────────────────────────
    // 4. Create or obtain an Arrow RecordBatch to upload
    // ────────────────────────────────────────────────────────────────
    let batch: RecordBatch = /* your RecordBatch here */ todo!();

    // ────────────────────────────────────────────────────────────────
    // 5. Write the batch as a Parquet file to MinIO
    // ────────────────────────────────────────────────────────────────
    write_parquet_to_minio(
        &batch,
        "table/date=2025-11-05/part-0001.parquet",
        &bucket,
        &endpoint,
        &access_key,
        &secret_key,
        &region,
    )
    .await?;

    println!("✅ Parquet uploaded using .env + AWS_CA_BUNDLE");
    Ok(())
}